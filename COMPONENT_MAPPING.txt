================================================================================
PORTFOLIO PLANNER - COMPONENT & API ENDPOINT MAPPING
================================================================================

FILE LOCATIONS & ACTUAL COMPONENT IMPLEMENTATIONS

================================================================================
SKILLS MATRIX DUPLICATE CHECK
================================================================================

File 1: /pages/SkillsMatrixPage.tsx
  - Route: /skills-matrix (in App.tsx routes)
  - Sidebar: No direct link in navRegistry
  - Status: Standalone page

File 2: /pages/reports/ResourceSkillsMatrixPage.tsx
  - Route: /reports/skills-matrix (navRegistry: "Skills Matrix")
  - Sidebar: Yes, "Skills Matrix" in People group
  - Status: Primary sidebar link
  
VERDICT: TWO DIFFERENT FILES FOR SAME CONCEPT - Needs verification if they're identical

================================================================================
CAPACITY/WORKLOAD RELATED PAGES
================================================================================

Sidebar Items:
  1. Capacity Hub -> /people/capacity -> CapacityTabs.tsx
     Tabs within hub:
       - Capacity (default) -> CapacityDemandPage
       - Overrides -> OverridesPage
       - Leave -> LeaveManagementPage
  
  2. Capacity Demand -> /reports/capacity-demand -> CapacityDemandPage
     NOTE: SAME component as default tab in Capacity Hub
  
  3. Workload Chart -> /reports/workload-chart -> WorkloadChartPage.tsx
     NOTE: Different component but similar data domain
  
  4. Overrides -> /overrides -> OverridesPage.tsx
     NOTE: ALSO accessible as tabs in:
       - Capacity Hub (tab: overrides)
       - Resources Hub (tab: overrides)
  
  5. Leave Hub -> /leave -> LeaveHubPage.tsx
     NOTE: ALSO accessible as tab in Capacity Hub (tab: leave)
  
  6. Performance -> /people/performance -> PerformanceTabs.tsx
     NOTE: Could be related to capacity/resource planning

Files in /reports/:
  - CapacityDemandPage.tsx (duplicated via Capacity Hub tab)
  - CapacityForecastPage.tsx (referenced but no sidebar item)
  - CapacityGapPage.tsx (legacy, redirects to /reports/utilization)

VERDICT: HIGHLY FRAGMENTED - 6+ pieces of capacity-related functionality across sidebar

================================================================================
ENGINEERING HUB DUPLICATES
================================================================================

Sidebar Hub: Engineering Hub -> /engineering/hub -> EngineeringHubTabs.tsx
  Tabs within hub:
    - Intelligence (default) -> EngineeringIntelligencePage
    - DORA Metrics -> DoraMetricsPage
    - Delivery Predictability -> DeliveryPredictabilityPage

Standalone Sidebar Items (DUPLICATES):
  1. Engineering Intelligence -> /reports/engineering-intelligence -> EngineeringIntelligencePage
  2. DORA Metrics -> /reports/dora -> DoraMetricsPage
  3. Delivery Predictability -> /reports/delivery-predictability -> DeliveryPredictabilityPage

VERDICT: ALL 3 TABS ALSO EXIST AS STANDALONE SIDEBAR ITEMS - Classic tab hub duplication

================================================================================
PORTFOLIO HEALTH HUB DUPLICATES
================================================================================

Sidebar Hub: Portfolio Health -> /portfolio/health -> PortfolioHealthTabs.tsx
  Tabs within hub:
    - Overview (default) -> PortfolioHealthDashboardPage
    - Project Health -> ProjectHealthPage
    - Status Updates -> StatusUpdatesFeedPage

Standalone Sidebar Items (DUPLICATES):
  1. Project Health -> /reports/project-health -> ProjectHealthPage (IN "Portfolio" GROUP)
  2. Status Updates -> /reports/status-updates -> StatusUpdatesFeedPage (IN "Jira" GROUP)

VERDICT: 2 OF 3 TABS ALSO EXIST AS STANDALONE SIDEBAR ITEMS - High confusion

================================================================================
PEOPLE/RESOURCE HUBS DUPLICATES
================================================================================

Hub 1: Resources -> /people/resources -> ResourcesTabs.tsx
  Tabs within hub:
    - Directory (default) -> ResourcesPage
    - Availability -> AvailabilityPage
    - Bookings -> ResourceBookingsPage
    - Overrides -> OverridesPage

Standalone Sidebar Items (DUPLICATES):
  1. Resource Bookings -> /resource-bookings -> ResourceBookingsPage (IN "People" GROUP)
  2. Overrides -> /overrides -> OverridesPage (IN "People" GROUP)

Hub 2: Capacity Hub -> /people/capacity -> CapacityTabs.tsx
  Tabs within hub:
    - Capacity (default) -> CapacityDemandPage
    - Overrides -> OverridesPage (ALSO IN RESOURCES HUB!)
    - Leave -> LeaveManagementPage

Standalone Sidebar Items (DUPLICATES):
  1. Capacity Demand -> /reports/capacity-demand -> CapacityDemandPage
  2. Overrides -> /overrides -> OverridesPage (ALSO IN RESOURCES HUB + SIDEBAR!)
  3. Leave Hub -> /leave -> LeaveHubPage

Hub 3: Performance -> /people/performance -> PerformanceTabs.tsx
  Tabs within hub:
    - Resource Performance (default) -> ResourcePerformancePage
    - Resource Intelligence -> ResourceIntelligencePage

Standalone Sidebar Items (DUPLICATES):
  1. Resource Intelligence -> /reports/resource-intelligence -> ResourceIntelligencePage

Other Standalone Resource Items:
  1. Skills Matrix -> /reports/skills-matrix -> ResourceSkillsMatrixPage
  2. Team Pulse -> /reports/team-pulse -> TeamPulsePage
  3. Utilization -> /reports/utilization -> UtilizationCenterPage
  4. Hiring Forecast -> /reports/hiring-forecast -> HiringForecastPage
  5. POD Hours -> /reports/pod-hours -> PodHoursPage

VERDICT: EXTREME FRAGMENTATION - Overrides appears 3 different ways, multiple hubs all related to "people"

================================================================================
PORTFOLIO TIMELINE PAGES
================================================================================

Sidebar Items:
  1. Portfolio Timeline -> /portfolio/timeline -> PortfolioTimelineTabs.tsx
     Tabs within hub:
       - Portfolio Timeline (default) -> PortfolioTimelinePage
       - Gantt & Dependencies -> ProjectGanttPage
  
  2. Advanced Timeline -> /advanced-timeline -> AdvancedTimelinePage.tsx
     NOTE: Separate page not in any hub, overlaps with Portfolio Timeline
  
  3. Dependency Map -> /reports/dependency-map -> DependencyMapPage.tsx
     NOTE: Related to timeline dependencies
  
  4. Project POD Matrix -> /reports/project-pod-matrix -> ProjectPodMatrixPage.tsx
     NOTE: Related visualization

Orphaned Files:
  - PortfolioTimelinePage.tsx (used in tab)
  - ProjectGanttPage.tsx (used in tab, also file refs as GanttDependenciesPage)
  - GanttDependenciesPage.tsx (in /pages/ root, imported by App.tsx routes)
  - DependencyMapPage.tsx (/reports/)
  - CrossPodDependencyPage.tsx (/reports/)
  - CrossTeamDependencyPage.tsx (/reports/)
  - RoadmapTimelinePage.tsx (/reports/)
  - DependencyDagTab.tsx (/reports/)

VERDICT: 4+ timeline/gantt/dependency pages with overlapping functionality

================================================================================
JIRA DASHBOARD HUB DUPLICATES
================================================================================

Sidebar Hub: POD Dashboard -> /delivery/jira -> JiraDashboardTabs.tsx
  Tabs within hub:
    - POD Dashboard (default) -> PodDashboardPage
    - Jira Actuals -> JiraActualsPage
    - Support Queue -> JiraSupportPage
    - Worklog -> JiraWorklogPage

Standalone Page Files:
  1. PodDashboardPage.tsx (in /pages/)
  2. JiraActualsPage.tsx (in /pages/)
  3. JiraSupportPage.tsx (in /pages/)
  4. JiraWorklogPage.tsx (in /pages/)

Standalone Sidebar Items:
  1. Sprint Backlog -> /sprint-backlog -> SprintBacklogPage
  2. Sprint Retro -> /reports/sprint-retro -> SprintRetroPage
  3. Releases -> /delivery/releases -> ReleasesTabs.tsx
  4. Jira Analytics -> /reports/jira-analytics -> JiraAnalyticsPage
  5. Dashboard Builder -> /reports/jira-dashboard-builder -> JiraDashboardBuilderPage
  6. Portfolio Sync -> /reports/jira-portfolio-sync -> JiraPortfolioSyncPage
  7. Status Updates -> /reports/status-updates -> StatusUpdatesFeedPage

VERDICT: Jira section is SPLIT between hub (4 tabs) + 7 standalone sidebar items - inconsistent pattern

================================================================================
RELEASES/NOTES PAGES
================================================================================

Hub: Releases -> /delivery/releases -> ReleasesTabs.tsx
  Tabs within hub:
    - Releases (default) -> ReleasesPage
    - Release Notes -> ReleaseNotesPage

Page Files:
  - ReleasesPage.tsx
  - ReleaseNotesPage.tsx

VERDICT: Clean tab hub - no duplicate sidebar items for these

================================================================================
SCENARIO/SIMULATOR PAGES
================================================================================

Hub: Scenario Tools -> /tools/scenarios -> ScenarioToolsTabs.tsx
  Tabs within hub:
    - Timeline Simulator (default) -> TimelineSimulatorPage
    - Scenario Simulator -> ScenarioSimulatorPage

Orphaned Files:
  - /simulators/TimelineSimulatorPage.tsx
  - /simulators/ScenarioSimulatorPage.tsx
  - ScenarioPlanningPage.tsx (sidebar item reference but route not clear)

VERDICT: Scenario pages consolidated into hub, but ScenarioPlanningPage exists separately

================================================================================
SETTINGS PAGES MISMATCH
================================================================================

SettingsHubPage.tsx shows 12 categories as cards:
  1. Organisation -> /settings/org -> OrgSettingsPage
  2. Users & Roles -> /settings/users -> UserManagementPage
  3. Jira Integration -> /settings/jira -> JiraSettingsPage (also leads to credentials, mapping)
  4. Email & Notifications -> /settings/email-templates -> EmailTemplatesPage
  5. Webhooks -> /settings/webhooks -> WebhookSettingsPage
  6. Scheduled Reports -> /settings/scheduled-reports -> ScheduledReportsPage
  7. Cost Rates -> /settings/cost-rates -> CostRatesPage
  8. Custom Fields -> /settings/custom-fields -> CustomFieldsAdminPage
  9. Azure DevOps -> /settings/azure-devops -> AzureDevOpsSettingsPage
  10. AI & NLP -> /settings/nlp -> NlpSettingsPage
  11. Audit Log -> /settings/audit-log -> AuditLogPage
  12. Reference Data -> /settings/ref-data -> RefDataSettingsPage

But navRegistry lists 23 settings items including:
  - Sidebar Order -> /settings/sidebar-order
  - NLP Optimizer -> /settings/nlp-optimizer
  - Smart Mapping -> /settings/smart-mapping
  - Timeline Settings -> /settings/timeline
  - DB Tables -> /settings/tables
  - Error Logs -> /settings/error-logs
  - Release Settings -> /settings/releases
  - Support Boards -> /settings/support-boards
  - Jira Credentials -> /settings/jira-credentials
  - Release Mapping -> /settings/jira-release-mapping
  - Resource Mapping -> /settings/jira-resource-mapping
  - Notification Prefs -> /settings/notification-preferences
  - Changelog -> /settings/changelog
  - Feedback Hub -> /settings/feedback

VERDICT: Settings sidebar has 23 items but SettingsHubPage only shows 12 categories
- Mismatch between what's in navRegistry and what's in SettingsHubPage
- Not clear if all 23 are shown in sidebar or just subset

================================================================================
API ENDPOINT PATTERNS
================================================================================

Based on search in page files (limited data):

ProjectHealthPage likely uses:
  - useProjects()
  - useProjectPodMatrix()
  - useUtilizationHeatmap()

PortfolioHealthDashboardPage likely uses:
  - useProjects()
  - useResources()
  - usePods()
  - useCapacityDemandSummary()

These appear to be different API calls, suggesting different data models for "health"

DeliveryPredictabilityPage (499 lines)
DoraMetricsPage (1161 lines)
  - Different file sizes suggest different implementations with different data

Workload related pages show pattern of multiple independent components for capacity

VERDICT: API patterns suggest these are GENUINELY different pages, not just styling differences

================================================================================
CRITICAL PATHS NEEDING CONSOLIDATION
================================================================================

Priority 1 - Collapse Immediate Duplicates:
  a) Remove sidebar items #41, #42, #43 (DORA, Engineering Intelligence, Delivery Predictability)
     -> Users should access from Engineering Hub tabs instead
  b) Remove sidebar item #14 (Project Health) 
     -> Users should access from Portfolio Health hub tab instead
  c) Remove sidebar item #51 (Status Updates)
     -> Users should access from Portfolio Health hub tab instead

Priority 2 - Consolidate Related Items:
  a) Capacity Hub group:
     - Remove sidebar #25 (Capacity Demand) - it's default tab in hub
     - Remove sidebar #28 (Overrides) - it appears in 2 hubs already
     - Consider merging Workload Chart into hub
  b) Resource Intelligence:
     - Remove sidebar #26 - it's tab in Performance Hub
  c) Resource Bookings:
     - Remove sidebar #29 - it's tab in Resources Hub

Priority 3 - Decide on Timelines:
  a) Advanced Timeline vs Portfolio Timeline Hub - decide which is canonical
  b) Dependency Map vs Gantt & Dependencies tab - consolidate

Priority 4 - Review Settings:
  a) Clarify which of 23 settings items are actually shown in sidebar
  b) Update SettingsHubPage to match navRegistry or vice versa

================================================================================
