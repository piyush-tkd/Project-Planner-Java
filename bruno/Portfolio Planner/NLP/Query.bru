meta {
  name: Query
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/nlp/query
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "query": "which pods are over capacity?"
  }
}
