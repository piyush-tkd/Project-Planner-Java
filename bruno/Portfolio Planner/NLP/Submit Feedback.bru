meta {
  name: Submit Feedback
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/nlp/feedback
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "queryLogId": 1,
    "rating": "POSITIVE",
    "comment": "Great answer!"
  }
}
