meta {
  name: Capacity Gap
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/reports/capacity-gap
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:query {
  ~unit: HOURS
}
