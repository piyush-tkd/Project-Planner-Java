meta {
  name: Create Override
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/overrides
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "resourceId": 1,
    "month": "2025-03",
    "overrideHours": 80,
    "reason": "Half-time on other project"
  }
}
