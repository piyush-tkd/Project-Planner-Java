meta {
  name: User History
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/jira/worklog/user-history?author=john.doe&months=6
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
