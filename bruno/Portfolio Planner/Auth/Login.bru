meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "admin"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("auth_token", res.body.token);
  }
}
