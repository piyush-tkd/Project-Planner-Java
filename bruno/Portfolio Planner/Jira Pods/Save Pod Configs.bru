meta {
  name: Save Pod Configs
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/jira/pods/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  [
    {
      "podId": 1,
      "boardId": 100
    }
  ]
}
