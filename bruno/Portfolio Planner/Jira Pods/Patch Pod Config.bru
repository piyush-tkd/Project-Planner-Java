meta {
  name: Patch Pod Config
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/api/jira/pods/config/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "boardId": 200
  }
}
