meta {
  name: Update Feedback
  type: http
  seq: 3
}

put {
  url: {{base_url}}/api/feedback/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "status": "IN_PROGRESS",
    "priority": "HIGH",
    "adminNotes": "Looking into this"
  }
}
