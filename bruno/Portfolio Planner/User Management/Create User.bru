meta {
  name: Create User
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "username": "newuser",
    "password": "password123",
    "displayName": "New User",
    "role": "READ_ONLY"
  }
}
