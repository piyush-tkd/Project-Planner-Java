meta {
  name: List Projects
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/projects
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:query {
  ~status: ACTIVE
}
