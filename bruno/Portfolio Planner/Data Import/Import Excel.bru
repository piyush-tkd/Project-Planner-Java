meta {
  name: Import Excel
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/data/import-excel
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:multipart-form {
  file: @file(/path/to/your/data.xlsx)
}
