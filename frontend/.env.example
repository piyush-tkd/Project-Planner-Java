# Copy this to .env.local for local development (not needed — Vite proxy handles /api locally)
# Set VITE_API_URL in Vercel dashboard for production deployments

# Example (your Railway backend URL, no trailing slash):
# VITE_API_URL=https://your-app.up.railway.app
